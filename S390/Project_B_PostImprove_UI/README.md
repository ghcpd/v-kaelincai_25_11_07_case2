Project B - Post-Improvement UI (Wizard + Lazy Loading)\n\nThis demo implements a wizard that prioritizes title/due in first step and lazy-loads tags, members, and attachments in later steps. \n\nHow to run:\n1. Windows PowerShell: `cd Project_B_PostImprove_UI`\n2. `setup.sh` (create venv & install dependencies)\n3. `run_tests.sh 1` (run tests)\n\nArtifacts:\n- `screenshots/` captures for each test case;\n- `logs/` has server logs: `log_post.txt`;\n- `results/` contains `results_post.json`.\n